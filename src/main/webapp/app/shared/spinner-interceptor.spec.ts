import { SpinnerInterceptor } from './spinner-interceptor';

describe('SpinnerInterceptor', () => {
  it('should create an instance', () => {
    expect(new SpinnerInterceptor()).toBeTruthy();
  });
});
